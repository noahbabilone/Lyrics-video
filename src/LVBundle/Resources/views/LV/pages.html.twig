{% extends "LVBundle::layout.html.twig" %}  {% block stylesheets %}      {{ parent() }}  {% endblock %}{% block lv_body %}    <div class="container">        <div class="row">            <div class="col-sm-12">                <hr>                <div class="row">                    {% if videos is defined  and videos %}                        {{ dump(videos) }}                        {% for video in videos %}                            <div class="col-sm-3">                                {#<a href="https://www.youtube.com/watch?v={{ video.id.videoId }}">#}                                <a href="{{ path("youtube_get_video",{"id": video.id.videoId,"keyword":keyword}) }}">                                    <div class="thumbnail" style="height: 230px;">                                        <img src="{{ video.snippet.thumbnails.medium.url }}" alt="...">                                        <div class="caption">                                            <h5>{{ video.snippet.title }} label</h5>                                        </div>                                    </div>                                </a>                                {#                                                                                        <ifram src="https://www.youtube.com/watch?v={{ video.id.videoId }}" frameborder="0" allowfullscreen></ifram>                                #}                            </div>                        {% endfor %}                        <div class="col-sm-12">                            {#<a href="{{ path("youtube_token", {"keyword": keyword, "token":videos.nextPageToken }) }}">Suivant</a>#}                        </div>                    {% endif %}                </div>            </div>        </div>    </div>{% endblock %}    {% block javascripts %}        {{ parent() }}    {% endblock %}